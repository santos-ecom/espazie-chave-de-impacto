RewriteEngine On

# Force HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Prioritize HTML
DirectoryIndex index.html index.php

# Redirect chaveshopify slug to root
RewriteRule ^chaveshopify/?$ / [L,R=301]
